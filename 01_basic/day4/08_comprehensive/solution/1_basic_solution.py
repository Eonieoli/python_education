# 1_basic_solution.py

"""
Day 4 - 8교시: 함수와 타입 힌트 종합 실습
🟢 기초 Problem - 타입 힌트가 있는 계산기 함수 모음 (정답)
"""

from typing import List, Dict, Optional


# ============================================
# 1. 기본 계산 함수
# ============================================

def add(a: int, b: int) -> int:
    """두 정수를 더합니다."""
    return a + b


def subtract(a: float, b: float) -> float:
    """두 실수를 뺍니다."""
    return a - b


def multiply(a: int, b: int) -> int:
    """두 정수를 곱합니다."""
    return a * b


# ============================================
# 2. 기본값이 있는 함수
# ============================================

def divide(a: float, b: float, decimal_places: int = 2) -> float:
    """
    두 실수를 나누고, 지정된 소수점 자리수로 반올림합니다.
    
    Args:
        a: 나눠지는 수
        b: 나누는 수
        decimal_places: 소수점 자리수 (기본값: 2)
    
    Returns:
        나눗셈 결과 (반올림됨)
    """
    result = a / b
    return round(result, decimal_places)


def power(base: int, exponent: int = 2) -> int:
    """
    밑과 지수를 받아 거듭제곱을 계산합니다.
    
    Args:
        base: 밑
        exponent: 지수 (기본값: 2, 제곱)
    
    Returns:
        거듭제곱 결과
    """
    return base ** exponent


# ============================================
# 3. 여러 값을 반환하는 함수
# ============================================

def all_operations(a: float, b: float) -> tuple[float, float, float, float]:
    """
    두 숫자의 사칙연산 결과를 모두 반환합니다.
    
    Args:
        a: 첫 번째 숫자
        b: 두 번째 숫자
    
    Returns:
        (덧셈, 뺄셈, 곱셈, 나눗셈) 결과의 튜플
    """
    add_result = a + b
    sub_result = a - b
    mul_result = a * b
    div_result = a / b
    
    return add_result, sub_result, mul_result, div_result


# ============================================
# 결과 확인
# ============================================

if __name__ == "__main__":
    print("=" * 50)
    print("기본 계산 함수 테스트")
    print("=" * 50)
    
    # 덧셈
    print(f"10 + 5 = {add(10, 5)}")  # 출력: 10 + 5 = 15
    
    # 뺄셈
    print(f"10.5 - 3.2 = {subtract(10.5, 3.2)}")  # 출력: 10.5 - 3.2 = 7.3
    
    # 곱셈
    print(f"7 * 8 = {multiply(7, 8)}")  # 출력: 7 * 8 = 56
    
    print("\n" + "=" * 50)
    print("기본값 함수 테스트")
    print("=" * 50)
    
    # 나눗셈 (기본값 사용)
    print(f"10 / 3 = {divide(10, 3)}")  # 출력: 10 / 3 = 3.33
    
    # 나눗셈 (소수점 4자리)
    print(f"10 / 3 = {divide(10, 3, 4)}")  # 출력: 10 / 3 = 3.3333
    
    # 거듭제곱 (기본값: 제곱)
    print(f"5^2 = {power(5)}")  # 출력: 5^2 = 25
    
    # 거듭제곱 (세제곱)
    print(f"5^3 = {power(5, 3)}")  # 출력: 5^3 = 125
    
    print("\n" + "=" * 50)
    print("여러 값 반환 함수 테스트")
    print("=" * 50)
    
    # 사칙연산 한번에
    add_result, sub_result, mul_result, div_result = all_operations(10, 2)
    print(f"10 + 2 = {add_result}")  # 출력: 10 + 2 = 12.0
    print(f"10 - 2 = {sub_result}")  # 출력: 10 - 2 = 8.0
    print(f"10 * 2 = {mul_result}")  # 출력: 10 * 2 = 20.0
    print(f"10 / 2 = {div_result}")  # 출력: 10 / 2 = 5.0
