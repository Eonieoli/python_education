# 3_advanced.py

"""
Day 4 - 8교시: 함수와 타입 힌트 종합 실습
🔴 도전 Problem - *args, **kwargs + 완벽한 타입 힌트

문제:
가변 인수를 활용하는 고급 함수들을 작성하세요.
*args, **kwargs를 사용하고 완벽한 타입 힌트를 추가하세요.

요구사항:
1. 가변 인수로 통계 계산
2. 키워드 인수로 설정 병합
3. 언패킹을 활용한 데이터 처리

제한 시간: 10분
난이도: 🔴 도전
"""

from typing import List, Dict, Optional, Any


# ============================================
# TODO 1: 가변 인수 통계 계산 (4분)
# ============================================

# 여러 개의 숫자를 받아서 합계, 평균, 최댓값, 최솟값을 계산하세요
# *args를 사용하여 개수 제한 없이 숫자를 받으세요
# 타입 힌트: *int -> Dict[str, float]
#
# 예시:
# calculate_stats(10, 20, 30, 40, 50)
# 결과: {
#     "sum": 150,
#     "average": 30.0,
#     "max": 50,
#     "min": 10
# }
def calculate_stats():
    pass


# ============================================
# TODO 2: 설정 병합 함수 (3분)
# ============================================

# 여러 개의 설정 딕셔너리를 받아서 하나로 병합하세요
# **kwargs를 사용하여 키워드 인수로 받으세요
# 나중에 전달된 값이 이전 값을 덮어씁니다
# 타입 힌트: **Any -> Dict[str, Any]
#
# 예시:
# merge_settings(host="localhost", port=8000, debug=True, port=3000)
# 결과: {"host": "localhost", "port": 3000, "debug": True}
# (port는 나중 값인 3000으로 덮어씀)
def merge_settings():
    pass


# ============================================
# TODO 3: API 요청 함수 (3분)
# ============================================

# API 엔드포인트와 여러 파라미터를 받아서 요청 정보를 생성하세요
# 위치 인수로 경로를, 키워드 인수로 쿼리 파라미터를 받으세요
# 타입 힌트: str, **str -> Dict[str, Any]
#
# 예시:
# build_api_request("/users", page=1, limit=10, sort="name")
# 결과: {
#     "endpoint": "/users",
#     "params": {"page": 1, "limit": 10, "sort": "name"}
# }
def build_api_request():
    pass


# ============================================
# 결과 확인
# ============================================

if __name__ == "__main__":
    print("=" * 50)
    print("1. 가변 인수 통계 계산")
    print("=" * 50)
    
    # stats = calculate_stats(10, 20, 30, 40, 50)
    # print(f"숫자: 10, 20, 30, 40, 50")
    # print(f"합계: {stats['sum']}")
    # print(f"평균: {stats['average']}")
    # print(f"최댓값: {stats['max']}")
    # print(f"최솟값: {stats['min']}")
    # 출력:
    # 합계: 150
    # 평균: 30.0
    # 최댓값: 50
    # 최솟값: 10
    
    print("\n" + "=" * 50)
    print("2. 설정 병합")
    print("=" * 50)
    
    # settings = merge_settings(
    #     host="localhost",
    #     port=8000,
    #     debug=True,
    #     port=3000  # port 덮어쓰기
    # )
    # print(f"병합된 설정: {settings}")
    # 출력: 병합된 설정: {'host': 'localhost', 'port': 3000, 'debug': True}
    
    print("\n" + "=" * 50)
    print("3. API 요청 정보 생성")
    print("=" * 50)
    
    # request1 = build_api_request("/users", page=1, limit=10)
    # print(f"요청 1: {request1}")
    # 출력: 요청 1: {'endpoint': '/users', 'params': {'page': 1, 'limit': 10}}
    
    # request2 = build_api_request("/posts", user_id=123, sort="date", order="desc")
    # print(f"요청 2: {request2}")
    # 출력: 요청 2: {'endpoint': '/posts', 'params': {'user_id': 123, 'sort': 'date', 'order': 'desc'}}
